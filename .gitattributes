# Normalize line endings: store text as LF in the repo, check out native.
* text=auto eol=lf

# Treat common binaries as binary (no EOL conversion / diff noise)
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.ico binary
*.pdf binary
*.joblib binary
*.jar binary
*.keystore binary
*.jks binary
